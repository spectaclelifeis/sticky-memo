# 📌 Sticky Memo App
파이썬 `tkinter`로 만든 깔끔한 스티커 메모 앱입니다.

## ✨ 주요 기능
- **자동 저장**: 입력하는 즉시 `memos.json`에 저장됩니다.
- **단축키 지원**:
  - `Ctrl+N`(새 메모), `Ctrl+F`(검색),
  - `e`(내보내기), `i`(불러오기)
  - `a`(가나다순), `s`(최신순), `d`(오래된순), `f`(수정된순)

## 🚀 실행 방법
```bash
python app.py
